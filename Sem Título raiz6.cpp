#include <stdio.h>
#include <math.h>
main()
{ float  numero , raiz = 0;
 printf("Digite um numero ");
 scanf("%f" ,& numero);
 if (numero > 0);
 { raiz = sqrt(numero);
  printf("\n A raiz desse = %0.2f",raiz);
 }
 
}
